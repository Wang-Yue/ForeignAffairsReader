import SwiftUI

@main
struct ForeignAffairsReaderApp: App {
  var body: some Scene {
    WindowGroup {
      ContentView()
        .frame(minWidth: 850, minHeight: 600)
    }
    .windowStyle(.hiddenTitleBar)
  }
}
